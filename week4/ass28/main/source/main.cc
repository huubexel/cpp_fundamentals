#include "../internal_header/main.ih"

// TEMPORARY

int main()
{
    Line line{ "", 0 };
    line.getLine();
    cout << line.next() << '\n';
    cout << line.next() << '\n';
    cout << line.next() << '\n';
}
