#include "../internal_header/charcount.ih"

int main()
{
    // string textString = readTestFile();
    // for (char ch: textString)
    //     showChar(ch);

    CharCount cc{};
    cout << cc.count() << '\n';
}